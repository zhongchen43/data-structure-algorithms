#include <stdio.h>

/*
//paeudo-code
ptr linkedListReverse(ptr head, int k)
{
    count = 1;
    new = head->next;
    old = new->next;
    while(count < k)
    {
        tmp = old->next;
        old->next = new;
        new = old;
        old = tmp;
        count++;
    }
    head->next->next = old;
    return new;
}
*/

int main()
{
    printf("Hello World!\n");
    return 0;
}
